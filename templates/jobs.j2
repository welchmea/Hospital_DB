{% include "index.html" %}
<body>
<main>
<!-- home page Jobs-->
   <div class="table-div">
    <table>
    <caption><strong>Current Job Data</strong></caption>
    {% if jobs_data %}
        <thead>
            <tr>
                {% for key in jobs_data[0] %}
                <th scope='col' data-field='{{ key }}' data-filter-control='input'>{{ key }}</th>
                {% endfor %}
                <th></th>
                <th></th>
            </tr>
        <tbody>
            {% for data in jobs_data %}
            <tr>
                {% for i in data %}
                <td> {{i}} </td>
                {% endfor %}

                <td><a  class="update-delete-btn"  href='edit_job/{{data[0]}}'>Update</a></td>
                <td><a  class="update-delete-btn"  href="delete_jobs/{{data[0]}}">Delete</a></td>

            </tr>
            {% endfor %}
        {% endif %}
    </table>
    <a class="add-btn" href='add_job'>Add New Job</a>
</div>
</main> 
</body>
</html>